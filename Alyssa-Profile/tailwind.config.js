// Import the default theme from Tailwind
import defaultTheme from 'tailwindcss/defaultTheme'; // <-- ADD THIS LINE

/** @type {import('tailwindcss').Config} */
export default {
    content: [
      "./index.html",
      "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
      container: {
        center: true,
        padding: "2rem",
        screens: {
          "2xl": "1400px",
        },
      },
      extend: { // Make sure you are inside the 'extend' object

        // 👇 ADD THIS fontFamily SECTION 👇
        fontFamily: {
          sans: ["Inter", ...defaultTheme.fontFamily.sans], // Use Inter as default sans
          serif: ["Playfair Display", ...defaultTheme.fontFamily.serif], // Use Playfair for font-serif
        },
        // 👆 END OF fontFamily SECTION 👆

        // Your existing configurations below:
        colors: {
          border: "hsl(var(--border))",
          input: "hsl(var(--input))",
          ring: "hsl(var(--ring))",
          background: "hsl(var(--background))",
          foreground: "hsl(var(--foreground))",
          primary: {
            DEFAULT: "hsl(var(--primary))",
            foreground: "hsl(var(--primary-foreground))",
          },
          secondary: {
            DEFAULT: "hsl(var(--secondary))",
            foreground: "hsl(var(--secondary-foreground))",
          },
          destructive: {
            DEFAULT: "hsl(var(--destructive))",
            foreground: "hsl(var(--destructive-foreground))",
          },
          muted: {
            DEFAULT: "hsl(var(--muted))",
            foreground: "hsl(var(--muted-foreground))",
          },
          accent: {
            DEFAULT: "hsl(var(--accent))",
            foreground: "hsl(var(--accent-foreground))",
          },
          popover: {
            DEFAULT: "hsl(var(--popover))",
            foreground: "hsl(var(--popover-foreground))",
          },
          card: {
            DEFAULT: "hsl(var(--card))",
            foreground: "hsl(var(--card-foreground))",
          },
        },
        borderRadius: {
          lg: "var(--radius)",
          md: "calc(var(--radius) - 2px)",
          sm: "calc(var(--radius) - 4px)",
        },
        keyframes: {
          "accordion-down": {
            from: { height: "0" },
            to: { height: "var(--radix-accordion-content-height)" },
          },
          "accordion-up": {
            from: { height: "var(--radix-accordion-content-height)" },
            to: { height: "0" },
          },
        },
        animation: {
          "accordion-down": "accordion-down 0.2s ease-out",
          "accordion-up": "accordion-up 0.2s ease-out",
        },
      }, // End of 'extend'
    }, // End of 'theme'
    // NOTE: You might need to install 'tailwindcss-animate' if not already done
    // If 'require' causes issues in your ESM setup, you might need to find an alternative way to include this plugin,
    // but often 'require' still works here for plugins.
    plugins: [require("tailwindcss-animate")],
  }